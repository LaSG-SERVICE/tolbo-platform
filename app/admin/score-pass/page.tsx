import config from "@/data/scorepass-v1.json";
export default function ScorePassAdmin(){const c=config as any;const dims=c.dimensions.filter((x:any)=>x.Code);const cal=c.calibration_items.filter((x:any)=>x.ID);const gates=c.hard_gates.filter((x:any)=>x.Code);return <><div className="section-title">Score Pass V1</div><p className="muted mt-1">Référentiel méthodologique chargé dans l'application.</p><div className="grid-auto mt-6">{[['Dimensions',dims.length],['Indicateurs',c.indicators.length],['Variables',c.variables.length],['Niveaux de preuve',c.evidence_levels.length],['Hard gates',gates.length],['À calibrer/finaliser',cal.length]].map(([a,b])=><div className="kpi" key={String(a)}><div className="small muted">{a}</div><div className="text-3xl font-extrabold mt-2">{b}</div></div>)}</div><div className="grid md:grid-cols-2 gap-6 mt-8"><div className="card p-6"><h2 className="font-bold">Pondérations D1–D6</h2><div className="mt-4 grid gap-3">{dims.map((d:any)=><div key={d.Code} className="flex justify-between"><span>{d.Code} — {d.Dimension}</span><strong>{Number(d['Poids V1']||0)*100}%</strong></div>)}</div></div><div className="card p-6"><h2 className="font-bold">IQP</h2><div className="mt-4 grid gap-3">{c.iqp_components.filter((x:any)=>x.Code).map((x:any)=><div key={x.Code} className="flex justify-between"><span>{x.Code} — {x.Composante}</span><strong>{Number(x['Poids V1']||0)*100}%</strong></div>)}</div></div></div><div className="card p-6 mt-6"><h2 className="font-bold">Paramètres explicitement non finalisés</h2><p className="muted mt-2">Ils sont affichés pour gouvernance, mais aucune valeur absente du référentiel n'est inventée par le moteur.</p><div className="mt-4 grid gap-2">{cal.map((x:any)=><div key={x.ID} className="text-sm border-b pb-2"><strong>{x.ID}</strong> — {x.Paramètre} <span className="badge bg-amber-50 text-amber-700 ml-2">{x.Statut}</span></div>)}</div></div></>}
